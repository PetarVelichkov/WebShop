{% extends 'base.html.twig' %}

{% block main %}
    <div id="all">

    <div id="content">
        <div class="container">
            <div class="col-md-6">
                <div class="box">
                    <h2 class="text-muted text-center">Edit user</h2>
                    <hr/>
                    {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}
                    <div class="text-center">
                        <a class="btn btn-warning" href="{{ path('admin_list_users') }}"><span class="fa fa-undo"></span> Cancel</a>
                        <button type="submit" class="btn btn-primary"><span class="fa fa-edit"></span> Edit</button>
                    </div>
                </div>

                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
    </div>
{% endblock %}



